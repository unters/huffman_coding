#ifndef HUFFMAN_UTILITY
#define HUFFMAN_UTILITY

void
print_usage();

bool
has_huf_extension(const char *);

char *
add_huf_extension(const char *);

char *
remove_huf_extension(const char *);

#endif
